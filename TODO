When click on filter in gutter, select filter
Add in /data/ a non-filter.yml file with all other key to display printer
Add printer picture
Add Manufacturer page
Add Compare printer page
Add pagination
On filter value, if click on label, set checkbox (onSelectedFilterValue()) if selected, remove selected
On filtter value , remove value if can't be selected (no data found)
Allow sort table
